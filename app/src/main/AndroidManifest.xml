<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourcompany.android">

  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

  <!-- TODO Register permissions to receive booting event  -->
  <!-- TODO Register permissions for scheduling exact alarms  -->

  <application
      android:name="com.yourcompany.android.studdy.StuddyApplication"
      android:allowBackup="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true">
    <activity
        android:name="com.yourcompany.android.studdy.MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:screenOrientation="portrait"
        android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- TODO Register exact alarm broadcast receiver   -->
    <!-- TODO Register inexact alarm broadcast receiver   -->
    <!-- TODO Register broadcast receiver for boot completed event for rescheduling alarms -->

    <receiver android:name="com.yourcompany.android.studdy.alarm.AlarmNotificationDismissedBroadcastReceiver" />

  </application>

</manifest>